Run a comprehensive WHOIS lookup on $ARGUMENTS:
1. `whois $ARGUMENTS` — registration data
2. Check nameservers: `dig +short NS $ARGUMENTS`
3. Summarize: registrar, dates, nameservers, registrant (redacted is fine)

Present in a clean table format.
